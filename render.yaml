services:
  - type: web
    name: crm-automation
    env: docker
    plan: free
    buildCommand: docker build -t app .
    startCommand: uvicorn crm_automation.api:app --host 0.0.0.0 --port 80
    envVars:
      - key: PORT
        value: 80
