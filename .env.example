# Email para login (opcional, pode ser passado via CLI)
CRM_EMAIL=seu_email@exemplo.com

# Timeout padrão em milissegundos
DEFAULT_TIMEOUT=30000

# Headless mode (true/false) - CLI flag --headful tem precedência
HEADLESS=true
